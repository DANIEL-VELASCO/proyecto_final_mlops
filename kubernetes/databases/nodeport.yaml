apiVersion: v1
kind: Service
metadata:
  name: postgres-nodeport
  namespace: mlops
  annotations:
    description: "NodePort para acceso desde containers Docker del training DAG"
spec:
  type: NodePort
  selector:
    app: postgres
  ports:
    - port: 5432
      targetPort: 5432
      nodePort: 30432
