apiVersion: v1
kind: Service
metadata:
  name: locust-nodeport
  namespace: mlops
spec:
  type: NodePort
  selector:
    app: locust
  ports:
    - port: 8089
      targetPort: 8089
      nodePort: 30089
