apiVersion: v1
kind: Service
metadata:
  name: fastapi-nodeport
  namespace: mlops
spec:
  type: NodePort
  selector:
    app: fastapi
  ports:
    - port: 8000
      targetPort: 8000
      nodePort: 30800
